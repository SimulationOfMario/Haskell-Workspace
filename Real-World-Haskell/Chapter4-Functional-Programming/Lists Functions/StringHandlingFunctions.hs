import Data.List (lines, unlines, words, unwords)
-- For use this functions you should import this

-- * Lines function
-- Returns a list of subStrings from the input String using \n as the delimiter
-- ? lines :: String -> [String]

-- * Unlines function
-- Is the inverse function of Lines
-- ? unlines :: [String] -> String

-- * Words function
-- Returns a list of subStrings from the input String using \n, \r, \t and whitespaces as the delimiters
-- ? words :: String -> [String]

-- * Unwords function
-- Is the inverse function of words, the output is a String with whitespaces between words
-- ? unwords :: [String] -> String